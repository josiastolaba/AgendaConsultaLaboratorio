header 
  nav.navbar.navbar-expand-lg.bg-body-tertiary
        .container-fluid
          img(src="../images/logomedico3.png", alt="logo" width='100' heigth='100')
          button.navbar-toggler(type="button", data-bs-toggle="collapse", data-bs-target="#navbarNav", aria-controls="navbarNav", aria-expanded="false", aria-label="Toggle navigation")
            span.navbar-toggler-icon
          .collapse.navbar-collapse#navbarNav
            ul.navbar-nav
              li.nav-item
                a.nav-link(aria-current="page", href="/") Home
              if(usuario.rol === "Médico" || usuario.rol === "Secretario" || usuario.rol === "Administrador")
                li.nav-item
                  a.nav-link(aria-current="page", href="/turno/darturno") Turno
              if(usuario.rol === "Médico" || usuario.rol === "Secretario" || usuario.rol === "Administrador")
                li.nav-item
                  a.nav-link(aria-current="page", href="/agendas/crearAgenda") Agenda
              if(usuario.rol === "Administrador")
                li.nav-item
                  a.nav-link(aria-current="page", href="/usuario/listar") Usuario
              if(usuario.rol === "Administrador")
                li.nav-item
                  a.nav-link(aria-current="page", href="/medico/datos") Medico
              li.nav-item
                a.nav-link(aria-current="page")
                  if usuario
                    form(action="/inicio/cerrarSesion" method="POST")
                      p #{usuario.nombre} #{usuario.apellido} #{usuario.rol}
                      input(type='submit' value='Cerrar Sesión')
                  else
                    p No hay usuario autenticado
                  


