extends layout

block content
    .container.mt-5
      h2.mb-4 Crear Nueva Agenda
      include cardDia
      button( type="button" onClick="manejarDia()") Agregar
      #containerDia.text-dark.d-flex.justify-content-around
    script(src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js")
    script(src="/validaciones.js")
    script.
        let table = new DataTable('#turnoTable');

        
    script(src="/cardDia.js")